
const   seventhKyu = require("../levels/7kyu/7kyu"),
        sixthKye = require("../levels/6kyu/6kyu"),
        fifthKye = require("../levels/5kyu/5kyu"),
        fourthKye = require("../levels/4kyu/4kyu"),
        thirdKye = require("../levels/3kyu/3kyu"),
        secondKye = require("../levels/2kyu/2kyu"),
        firstKye = require("../levels/1kyu/1kyu");


describe("Tests", () => {
    test("Work", () => {
        expect(1 + 1).toEqual(2);
    })
    // test("Test function greeet", () => {
    //     expect(greet("Ryan")).toEqual(expect.stringContaining("Hello, Ryan how are you doing today?"))
    //     expect(greet("Shingles")).toEqual(expect.stringContaining("Hello, Shingles how are you doing today?"))
    // })

    // test("Least large", () => {
    //     expect(leastLarger([4, 1, 3, 5, 6], 0)).toBe(3)
    //     expect(leastLarger([4, 1, 3, 5, 6], 4)).toBe(-1)
    //     expect(leastLarger([1, 3, 5, 2, 4], 0)).toBe(3)
    // })

    // test("duplicateSandwich", () => {
    //     duplicateSandwich([0, 1, 2, 3, 4, 5, 6, 1, 7, 8]), [2, 3, 4, 5, 6]
    //     duplicateSandwich(["None", "Hello", "Example", "hello", "None", "Extra"]), ["Hello", "Example", "hello"]
    //     duplicateSandwich([0, 0]), []
    //     duplicateSandwich( [true, false, true]), [false] 
    //     duplicateSandwich("example"), "xampl"
    // })


    // test("Solve strings", () => {
        // expect(solve("code*s","codewars")).toBe(true);
        // expect(solve("codewar*s","codewars")).toBe(true);
        // expect(solve("code*warrior","codewars")).toBe(false);
        // expect(solve("c","c")).toBe(true);
        // expect(solve("*s","codewars")).toBe(true);
        // expect(solve("*s","s")).toBe(true);
        // expect(solve("s*","s")).toBe(true);
        // expect(solve("*q","q")).toBe(true);
        // expect(solve("aa","aaa")).toBe(false);
        // expect(solve("aa*","aaa")).toBe(true);
        // expect(solve("aaa","aa")).toBe(false);
        // expect(solve("aaa*","aa")).toBe(false);
        // expect(solve("*","codewars")).toBe(true);
        // expect(solve("*ase","wase")).toBe(true);
    // })
})














